# data-pipeline-project
A lightweight ETL pipeline that extracts live crypto prices from a public API, transforms them with pandas, and loads them into SQL Server — built as a first hands-on data engineering project.
